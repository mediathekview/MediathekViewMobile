class SearchObject {
  var channels = [];
  var topics = [];
  var titles = [];
  var descriptions = [];
  var generics = [];

  SearchObject(this.channels, this.topics, this.titles, this.descriptions,
      this.generics);
}
